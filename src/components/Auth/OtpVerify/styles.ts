import type { SxProps } from '@mui/material';

const styles: Record<string, SxProps> = {
  otpVerifyWrapper: {
    padding: '0 20px',
    margin: {
      xs: '45px auto 75px',
      tablet: '60px auto 47px',
    },
    textAlign: 'center',
  },
};

export default styles;
